

<%#= render '/searches/cform' %>
<%= render '/cargos/search' %>
<!--
<% if not @cargos.nil? %>
  <table>
    <tr>
      <th>货物名称</th>
      <th>出发</th>     
      <th>到达</th>     
      <th>发货日期</th>
      <th>重量</th>
      <th>发布日期</th>
      <th>货源状态</th>    
         <th>车主操作</th>
    </tr>


    <% @cargos.each do |cargo| %>
      <%unless (cargo.nil? )%>
        <tr>
          <td><%= cargo.cate_name %></td>
          <td><%= cargo.fcity_name %></td> 
          <td><%= cargo.tcity_name %></td>          
          <td><%= cargo.send_date %></td>
          <td><%= cargo.cargo_weight %></td>

          <td><%=cargo.created_at.to_s.slice(5,11) %></td>
          <td><%=cargo.status %></td>

          <td>
            <%unless session[:user_id].nil?%>
            <%if cargo.user_id==session[:user_id]%>
            货主
            <%else%>
            <a class="baojia" href="/cargos/<%=cargo.id%>/quotes/new">报价</a>
            <%end%>
            <%else%>
            登录后报价
            <%end%>
           </td>
               
        </tr>
      <% end %>
    <% end %>
  </table>

<%= will_paginate @cargos%>

  <%#=will_paginate @cargos %>
<%else%>
  <p>
    不好意思，没有对应的货源。
  </p>
<% end %>

<div class="caozuo_area" ></div>

<br />
<% unless @stock_cargo.nil? %>
  <div>
    <a  href="/cargos/new/<%=@stock_cargo.id%>">新增货源</a>
  </div>

<% end %>

-->
